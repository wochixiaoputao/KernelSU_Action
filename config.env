CONFIG_ENV=config.env

# 1. 内核源码：改为小米官方开源地址
KERNEL_SOURCE=https://github.com/MiCode/Xiaomi_Kernel_OpenSource

# 2. 分支：适配 HyperOS 和 MIUI 14 (Android 13/14)
# 如果你是旧版 MIUI 13，请手动改为 star-s-oss
KERNEL_SOURCE_BRANCH=star-t-oss

# 3. 配置文件：11 Ultra 专用配置
KERNEL_CONFIG=vendor/star_defconfig

# 4. 内核文件名：骁龙888通常使用 Image
KERNEL_IMAGE_NAME=Image

ARCH=arm64
ADD_LOCALVERSION_TO_FILENAME=true
EXTRA_CMDS:LD=ld.lld

# Clang 编译器设置
## Custom
USE_CUSTOM_CLANG=false
CUSTOM_CLANG_SOURCE=
CUSTOM_CLANG_BRANCH=

### if your set USE CUSTOM CLANG to false than DO NOT CHANGE CUSTOM CMDS
CUSTOM_CMDS:CLANG_TRIPLE=aarch64-linux-gnu-

## AOSP Clang
# 这里使用 r383902b 版本，这是编译骁龙888内核最稳的版本
CLANG_BRANCH=master-kernel-build-2022
CLANG_VERSION=r383902b

# GCC
ENABLE_GCC_ARM64=true
ENABLE_GCC_ARM32=true
USE_CUSTOM_GCC_64=false
CUSTOM_GCC_64_SOURCE=
CUSTOM_GCC_64_BRANCH=
CUSTOM_GCC_64_BIN=aarch64-linux-android-
USE_CUSTOM_GCC_32=false
CUSTOM_GCC_32_SOURCE=
CUSTOM_GCC_32_BRANCH=
CUSTOM_GCC_32_BIN=arm-linux-androideabi-

# KernelSU 设置 (关键！)
# 开启 KernelSU
ENABLE_KERNELSU=true

# 使用最新的 main 分支，或者填 v0.9.5 (越新越好)
KERNELSU_TAG=main

KSU_EXPECTED_SIZE=
KSU_EXPECTED_HASH=

# Configuration 编译参数
DISABLE-LTO=false

# ⚠️ 开启忽略警告 (非常重要，否则容易报错停止)
DISABLE_CC_WERROR=true

# ⚠️ 开启 Kprobes 和 Overlayfs (模块兼容性必需)
ADD_KPROBES_CONFIG=true
ADD_OVERLAYFS_CONFIG=true

APPLY_KSU_PATCH=true
REMOVE_UNUSED_PACKAGES=true

# AnyKernel3 打包设置
## 使用自定义 AnyKernel3 仓库来适配 11U
USE_CUSTOM_ANYKERNEL3=true
CUSTOM_ANYKERNEL3_SOURCE=https://github.com/osm0sis/AnyKernel3
CUSTOM_ANYKERNEL3_BRANCH=master

# Ccache 缓存 (加快二次编译)
ENABLE_CCACHE=true

# DTBO image
NEED_DTBO=false

# Build boot images (我们用 AnyKernel3 卡刷包，所以这里关掉)
BUILD_BOOT_IMG=false
SOURCE_BOOT_IMAGE=
